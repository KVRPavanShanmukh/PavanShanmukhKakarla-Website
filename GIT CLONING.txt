For cloning the remote repostory:

1st way is using the command called git clone url
2nd way is using the command called git remote add origin url

COMMANDS:

$git config --list

$git config --global user.name "KVRPavanShanmukh" (github.com username ivvali)

$git config --global user.mail "Give the mail that you have logged in to github.com account"

$git init

$git remote add origin httpslink

$git status

$git pull origin main

$git add

$git commit -m "added gitcommands and coel files added"

$git push -u origin main